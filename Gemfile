source 'https://rubygems.org'

# Use the GitHub Pages gem to simplify dependencies
# This gem includes Jekyll and other supported plugins.
gem 'github-pages', group: :jekyll_plugins

# These are for the external posts plugin
group :other_plugins do
    gem 'httparty'
    gem 'feedjira'
end

gem 'webrick'